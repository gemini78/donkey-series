{% extends "base.html.twig" %} {% block body %}
<div class="container">
  <h1 class="my-4 text-center">New Category</h1>
  <div class="row">
    <div class="col-md-6 mx-auto">
      {{ form_start(form) }}
      {{ form_widget(form) }}
      {{ form_end(form) }}
      <div class="d-grid">
        <button class="btn btn-outline-secondary" type="submit">
          Add category
        </button>
      </div>
    </div>
  </div>
</div>

{% endblock %}
