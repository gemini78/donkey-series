{% extends 'base.html.twig' %} {% block title %}Edit Episode {% endblock %} {%
block body %}
<div class="container">
  <div class="row">
    <div class="col-md-8 mx-auto">
      <h1 class="my-4 text-center">Edit Episode</h1>
      {{ include("episode/_form.html.twig", { button_label: "Update" }) }}
      <div class="d-grid mt-3">
        <a class="btn btn-outline-primary" href="{{ path('episode_index') }}"
          >back to list</a
        >
      </div>

      {{ include("episode/_delete_form.html.twig") }}
    </div>
  </div>
</div>
{% endblock %}
