{% extends 'base.html.twig' %} {% block title %}New Episode {% endblock %} {%
block body %}
<div class="container">
  <div class="row">
    <div class="col-md-8 mx-auto">
      <h1 class="my-4 text-center">Create new Episode</h1>

      {{ include("episode/_form.html.twig") }}
      <div class="d-grid mt-3">
        <a class="btn btn-outline-primary" href="{{ path('episode_index') }}"
          >back to list</a
        >
      </div>
    </div>
  </div>
</div>
{% endblock %}
